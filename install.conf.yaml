- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- create:
    - ~/workspace

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

